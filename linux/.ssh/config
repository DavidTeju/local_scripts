Host *
    AddKeysToAgent yes
    # UseKeychain is macOS-only; on Linux use ssh-agent (gnome-keyring, KWallet, or `ssh-add`).

# UndocuStudent VPS
Host projects
    HostName 82.29.152.139
    User root
    IdentityFile ~/.ssh/id_ed25519

# Openclaw VPS (Tailscale only - public SSH firewalled)
Host openclaw
    HostName 100.73.19.15
    User root
    IdentityFile ~/.ssh/id_openclaw
